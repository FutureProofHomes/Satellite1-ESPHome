substitutions:
  # Phases of the Voice Assistant
  # The voice assistant is ready to be triggered by a wake word
  voice_assist_idle_phase_id: '1'
  # The voice assistant is waiting for a voice command (after being triggered by the wake word)
  voice_assist_waiting_for_command_phase_id: '2'
  # The voice assistant is listening for a voice command
  voice_assist_listening_for_command_phase_id: '3'
  # The voice assistant is currently processing the command
  voice_assist_thinking_phase_id: '4'
  # The voice assistant is replying to the command
  voice_assist_replying_phase_id: '5'
  # The voice assistant is not ready
  voice_assist_not_ready_phase_id: '10'
  # The voice assistant encountered an error
  voice_assist_error_phase_id: '11'

globals:
  # Global variable tracking the phase of the voice assistant (defined above). Initialized to not_ready
  - id: voice_assistant_phase
    type: int
    restore_value: no
    # initial_value: ${voice_assist_not_ready_phase_id}

microphone:
  - platform: satellite1
    id: sat1_mics
    sample_rate: 48000
    i2s_din_pin: GPIO15
    bits_per_sample: 32bit
    channel: stereo
    pdm: false
    adc_type: external
    i2s_audio_id: i2s_shared

micro_wake_word:
  id: mww
  models:
    - model: https://raw.githubusercontent.com/artfix/microWakeWords/main/microWakeWords/kira.json
      id: kira
    - model: https://raw.githubusercontent.com/artfix/microWakeWords/main/microWakeWords/hey_kira.json
      id: hey_kira
    - model: https://github.com/kahrendt/microWakeWord/releases/download/stop/stop.json
      id: stop
      internal: true
  vad:
  microphone:
    microphone: sat1_mics
    channels: 1
    gain_factor: 8
  stop_after_detection: false
  on_wake_word_detected:
    # If the wake word is detected when the device is muted (Possible with the software mute switch): Do nothing
    - if:
        condition:
          switch.is_off: master_mute_switch
        then:
          # If a timer is ringing: Stop it, do not start the voice assistant (We can stop timer from voice!)
          - if:
              condition:
                switch.is_on: timer_ringing
              then:
                - switch.turn_off: timer_ringing
              # Stop voice assistant if running
              else:
                - if:
                    condition:
                      voice_assistant.is_running:
                    then:
                      voice_assistant.stop:
                    # Stop any other media player announcement
                    else:
                      - if:
                          condition:
                            media_player.is_announcing:
                          then:
                            - media_player.stop:
                                announcement: true
                          # Start the voice assistant and play the wake sound, if enabled
                          else:
                            - if:
                                condition:
                                  switch.is_on: wake_sound
                                then:
                                  - lambda: |-
                                      std::string url = id(wake_word_sound_url).state;
                                      // Log the URL for debugging (remove after testing)
                                      ESP_LOGI("wake_word", "URL from box: '%s'", url.c_str());
                                      
                                      // Check if URL is valid (not empty and looks like a URL)
                                      if (!url.empty() && 
                                          url != "unknown" && 
                                          url != "none" &&
                                          (url.find("http://") == 0 || 
                                           url.find("https://") == 0 || 
                                           url.find("www.") == 0)) {
                                        // Play the URL directly
                                        id(external_media_player)->make_call()
                                          .set_media_url(url)
                                          .set_announcement(true)
                                          .perform();
                                      } else {
                                        // Play the default sound file
                                        id(external_media_player)->make_call()
                                          .set_media_url("wake_word_triggered_sound")
                                          .set_announcement(true)
                                          .perform();
                                      }
                                  - delay: 300ms
                            - voice_assistant.start:
                                wake_word: !lambda return wake_word;

voice_assistant:
  id: va
  microphone:
    microphone: sat1_mics
    channels: 0

  media_player: external_media_player
  micro_wake_word: mww
  use_wake_word: false
  noise_suppression_level: 0
  auto_gain: 0 dbfs
  volume_multiplier: 1.6
  on_client_connected:
    - lambda: id(init_in_progress) = false;
    - micro_wake_word.start:
    - lambda: id(voice_assistant_phase) = ${voice_assist_idle_phase_id};
    - script.execute: control_leds
  on_client_disconnected:
    - voice_assistant.stop:
    - lambda: id(voice_assistant_phase) = ${voice_assist_not_ready_phase_id};
    - script.execute: control_leds
  on_error:
    - if:
        condition:
          and:
            - lambda: return !id(init_in_progress);
            - lambda: return code != "duplicate_wake_up_detected";
        then:
          - lambda: id(voice_assistant_phase) = ${voice_assist_error_phase_id};
          - script.execute: control_leds
    # Stop ducking audio.
    - mixer_speaker.apply_ducking:
        id: media_mixing_input
        decibel_reduction: 0   # 0 dB means no reduction
        duration: 1.0s

  # When the voice assistant starts: Play a wake up sound, duck audio.
  on_start:
    - mixer_speaker.apply_ducking:
        id: media_mixing_input
        decibel_reduction: 20   # Number of dB quieter; higher implies more quiet, 0 implies full volume
        duration: 0.0s          # The duration of the transition (default is 0)
  on_listening:
    - lambda: id(voice_assistant_phase) = ${voice_assist_waiting_for_command_phase_id};
    - script.execute: control_leds
  on_stt_vad_start:
    - lambda: id(voice_assistant_phase) = ${voice_assist_listening_for_command_phase_id};
    - script.execute: control_leds
  on_stt_vad_end:
    - lambda: id(voice_assistant_phase) = ${voice_assist_thinking_phase_id};
    - script.execute: control_leds
  on_intent_progress:
    - if:
        condition:
          # A nonempty x variable means a streaming TTS url was sent to the media player
          lambda: 'return !x.empty();'
        then:
          - lambda: id(voice_assistant_phase) = ${voice_assist_replying_phase_id};
          - script.execute: control_leds
          # Start a script that would potentially enable the stop word if the response is longer than a second
          - script.execute: activate_stop_word_once
  on_tts_start:
    - if:
        condition:
          # The intent_progress trigger didn't start the TTS Reponse
          lambda: 'return id(voice_assistant_phase) != ${voice_assist_replying_phase_id};'
        then:
          - lambda: id(voice_assistant_phase) = ${voice_assist_replying_phase_id};
          - script.execute: control_leds
          # Start a script that would potentially enable the stop word if the response is longer than a second
          - script.execute: activate_stop_word_once
  # When the voice assistant ends ...
  on_end:
    - wait_until:
        not:
          voice_assistant.is_running:
    # Stop ducking audio.
    - mixer_speaker.apply_ducking:
        id: media_mixing_input
        decibel_reduction: 0   # 0 dB means no reduction
        duration: 1.0s
    # Stop the script that would potentially enable the stop word if the response is longer than a second
    - script.stop: activate_stop_word_if_tts_step_is_long
    # Disable the stop word (If the tiemr is not ringing)
    - if:
        condition:
          switch.is_off: timer_ringing
        then:
    # - lambda: id(stop).disable();
    # If the end happened because of an error, let the error phase on for a second
    - if:
        condition:
          lambda: return id(voice_assistant_phase) == ${voice_assist_error_phase_id};
        then:
          - delay: 1s
    # Reset the voice assistant phase id and reset the LED animations.
    - lambda: id(voice_assistant_phase) = ${voice_assist_idle_phase_id};
    - script.execute: control_leds

  on_timer_finished:
    - switch.turn_on: timer_ringing
  on_timer_started:
    - script.execute: control_leds
  on_timer_cancelled:
    - script.execute: control_leds
  on_timer_updated:
    - script.execute: control_leds
  on_timer_tick:
    - script.execute: control_leds

script:
  # Script used activate the stop word if the TTS step is long.
  # Why is this wrapped on a script?
  #   Becasue we want to stop the sequence if the TTS step is faster than that.
  #   This allows us to prevent having the deactivation of the stop word before its own activation.
  - id: activate_stop_word_if_tts_step_is_long
    then:
      - delay: 1s
      # Enable stop wake word
      # - lambda: id(stop).enable();

  - id: activate_stop_word_once
    then:
      - delay: 1s
      # Enable stop wake word
      - if:
          condition:
            switch.is_off: timer_ringing
          then:
            - micro_wake_word.enable_model: stop
            - wait_until:
                not:
                  media_player.is_announcing:
            - if:
                condition:
                  switch.is_off: timer_ringing
                then:
                  - micro_wake_word.disable_model: stop

select:
  # Selector for micro wake word 1
  - platform: template
    id: mww_sensitivity_select_1
    name: "MWW sensitivity 1"
    optimistic: true
    initial_option: "Moderately sensitive (210)"
    restore_value: true
    entity_category: config
    options:
      - Ultra insensitive (250)       # Extremely strict
      - Maximum insensitive (240)     # Very strict
      - Less insensitive (235)        # Strict
      - Slightly insensitive (230)    # A bit strict
      - Least insensitive (225)       # Barely strict
      - Moderately sensitive (210)    # Balanced
      - Slightly sensitive (200)      # A bit responsive
      - Least sensitive (190)         # Responsive
      - Very sensitive (180)          # Very responsive
      - Extremely sensitive (170)     # Most responsive
      - Maximum sensitive (160)       # Ultra responsive
    on_value:
      # Sets specific wake word probabilities computed for each particular model
      # Note probability cutoffs are set as a quantized uint8 value, each comment has the corresponding floating point cutoff
      # These cutoffs apply only to the specific models included in the firmware: kira, hey_kira
      # If you add more mww, you will need to add there names down here or replace kira and hey_kira
      lambda: |-
        if (x == "Ultra insensitive (250)") {
          id(kira).set_probability_cutoff(250);
        } else if (x == "Maximum insensitive (240)") {
          id(kira).set_probability_cutoff(240);
        } else if (x == "Less insensitive (235)") {
          id(kira).set_probability_cutoff(235);
        } else if (x == "Slightly insensitive (230)") {
          id(kira).set_probability_cutoff(230);
        } else if (x == "Least insensitive (225)") {
          id(kira).set_probability_cutoff(225);
        } else if (x == "Moderately sensitive (210)") {
          id(kira).set_probability_cutoff(210);
        } else if (x == "Slightly sensitive (200)") {
          id(kira).set_probability_cutoff(200);
        } else if (x == "Least sensitive (190)") {
          id(kira).set_probability_cutoff(190);
        } else if (x == "Very sensitive (180)") {
          id(kira).set_probability_cutoff(180);
        } else if (x == "Extremely sensitive (170)") {
          id(kira).set_probability_cutoff(170);
        } else if (x == "Maximum sensitive (160)") {
          id(kira).set_probability_cutoff(160);
        }

  # Selector for micro wake word 2
  - platform: template
    id: mww_sensitivity_select_2
    name: "MWW sensitivity 2"
    optimistic: true
    initial_option: "Moderately sensitive (210)"
    restore_value: true
    entity_category: config
    options:
      - Ultra insensitive (250)
      - Maximum insensitive (240)
      - Less insensitive (235)
      - Slightly insensitive (230)
      - Least insensitive (225)
      - Moderately sensitive (210)
      - Slightly sensitive (200)
      - Least sensitive (190)
      - Very sensitive (180)
      - Extremely sensitive (170)
      - Maximum sensitive (160)
    on_value:
      # Sets specific wake word probabilities computed for each particular model
      # Note probability cutoffs are set as a quantized uint8 value, each comment has the corresponding floating point cutoff
      # These cutoffs apply only to the specific models included in the firmware: kira, hey_kira
      # If you add more mww, you will need to add there names down here or replace kira and hey_kira
      lambda: |-
        if (x == "Ultra insensitive (250)") {
          id(hey_kira).set_probability_cutoff(250);
        } else if (x == "Maximum insensitive (240)") {
          id(hey_kira).set_probability_cutoff(240);
        } else if (x == "Less insensitive (235)") {
          id(hey_kira).set_probability_cutoff(235);
        } else if (x == "Slightly insensitive (230)") {
          id(hey_kira).set_probability_cutoff(230);
        } else if (x == "Least insensitive (225)") {
          id(hey_kira).set_probability_cutoff(225);
        } else if (x == "Moderately sensitive (210)") {
          id(hey_kira).set_probability_cutoff(210);
        } else if (x == "Slightly sensitive (200)") {
          id(hey_kira).set_probability_cutoff(200);
        } else if (x == "Least sensitive (190)") {
          id(hey_kira).set_probability_cutoff(190);
        } else if (x == "Very sensitive (180)") {
          id(hey_kira).set_probability_cutoff(180);
        } else if (x == "Extremely sensitive (170)") {
          id(hey_kira).set_probability_cutoff(170);
        } else if (x == "Maximum sensitive (160)") {
          id(hey_kira).set_probability_cutoff(160);
        }

text:
  - platform: template
    name: "Wake Word Sound URL"
    id: wake_word_sound_url
    entity_category: config
    optimistic: true
    restore_value: true
    mode: text
